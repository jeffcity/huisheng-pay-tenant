---
title: 商户管理优化需求
date: 2026-09-25
status: draft
tags: [汇盛支付, 租户端, 商户管理]
---

# 商户管理优化需求

## 目录

- [[#§1 需求说明]]
- [[#§2 背景与目标]]
- [[#§3 功能清单]]
- [[#§4 功能规则]]
- [[#§8 异常与边界]]
- [[#§9 验收清单]]

使用说明：本文件为简短业务需求；实现任务见 [[开发任务摘要_商户管理优化]]。Demo 用于交互评审，不代表真实账务或安全服务已交付。

## §1 需求说明

- 范围：租户端「商户管理」列表、钱包明细、商户详情的代收配置、代付配置、钱包账户及接入信息。
- 本次不包含：登录页、侧栏结构、资金申请审批规则、真实支付通道充值或提现。
- [MOD] 对照：[本轮修改前的 Demo 源](https://github.com/jeffcity/huisheng-pay-tenant/blob/dcc4e62753f62a7d5ea024863b8b751d933421d4/public/legacy/sources/merchants.html)。

## §2 背景与目标

- 面向租户运营及财务人员，集中完成商户接入配置和钱包人工调账。
- 统一资金操作入口及余额口径，减少重复入口，清晰区分回调 IP 与 API 请求白名单。

## §3 功能清单

| 类型 | 页面／能力 | 本次交付 |
| --- | --- | --- |
| [MOD] | 商户列表与详情 | 删除「业务视图」筛选；「跑量视图」更名「跑量统计」 |
| [NEW] | 钱包资金调整 | 四处入口统一支持手动充值、手动扣款 |
| [MOD] | 申请记录入口 | 移除钱包及业务配置内快捷入口，统一从「商户资金申请」查看 |
| [MOD] | 接入信息 | 多条 API IP 白名单增删改；按钮改为「保存」 |
| [MOD]/[NEW] | 代收／代付配置 | 代收回调 IP 可编辑，代付新增回调 IP 配置，分别保存 |

## §4 功能规则

### 列表与导航

- 保留商户关键词、类型、业务能力、状态、创建时间筛选；不改其他条件原有规则。
- 删除「业务视图」控件和关联筛选；申请记录本身及「商户资金申请」页面继续保留。

### 资金调整

- 入口：钱包明细、详情「钱包账户」、代收钱包余额、代付钱包余额；自动锁定当前商户、币种和钱包。
- 流程：选择手动充值／手动扣款 → 填写金额、原因和 Google 2FA → 核对调整前后余额 → 确认执行。
- 可用余额 = 当前总额 − 冻结金额；充值使总额和可用余额同额增加，扣款使两者同额减少，冻结金额不变。
- 金额须大于 0，最多两位小数；扣款不超过可用余额。保存调整流水及操作日志，各入口同步显示新余额。
- 完成或取消后返回原入口；取消不记账。本操作为商户内部人工记账，不调用支付通道。

### API 请求白名单

- 一行一项，支持新增、编辑、删除；支持公网 IPv4、IPv6 及明确网段。
- 修改时保留格式、重复项及 Google 2FA 校验；「保存」仅更名，不取消校验和确认步骤。
- 全部留空表示不限制请求 IP，确认时明确显示；保存成功后重新进入可回显。

### 回调 IP

- 代收和代付分别配置，支持多个公网 IPv4／IPv6，以逗号或换行分隔，不接受网段。
- 随对应业务配置保存，变更原因必填；保留格式、重复项校验和变更日志。
- 留空表示该业务回调 IP 未配置；未开通业务沿用不可编辑状态。
- 两类回调 IP 与 API 请求白名单独立维护，任一修改不得覆盖其他配置或其他商户。

## §8 异常与边界

- 金额错误、扣款余额不足、原因为空、2FA 错误：提示原因且不改变余额。
- 确认时余额发生变化：拒绝提交并要求重新核对；重复确认不得重复记账。
- IP 无效、重复或回调 IP 包含网段：阻止保存并定位字段；无变更时提示无需保存。
- 删除全部白名单与清空回调 IP 含义不同，不能混用；记录入口删除不删除历史数据。

## §9 验收清单

### 9.1 功能

- [ ] 列表无「业务视图」，详情显示「跑量统计」，接入保存按钮显示「保存」。
- [ ] 四处资金调整入口均带入正确商户和钱包；取消及成功返回原页签。
- [ ] 充值／扣款 100 元后，总额和可用余额分别同增／同减 100 元，冻结金额不变；生成一笔调整记录。
- [ ] 钱包与代收／代付配置无「申请记录」快捷入口，侧栏资金申请页面仍可访问。
- [ ] 多条白名单保存回显正常；代收和代付回调 IP 可分别保存及清空，互不覆盖。

### 9.2 基础质量

- [ ] 延续原有响应式布局与键盘操作；错误提示明确；生产环境按租户及操作权限隔离。

### 9.3 边界

- [ ] 金额精度、余额不足、错误 2FA、重复提交、错误／重复 IP 均按 §8 处理，失败不产生账务变更。

### 9.4 交付区分

- 本地 Demo 已通过构建及交互回归；正式开发需另行完成真实校验、持久化、并发记账及权限验收。

下一步：按 [[开发任务摘要_商户管理优化]] 分派正式开发与测试任务。
