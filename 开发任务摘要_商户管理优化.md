# 商户管理优化｜开发任务摘要

关联需求：[[PRD_商户管理优化_20260925]]。

状态说明：Demo 交互已实现；以下复选框表示正式开发与验收任务，不代表生产能力已完成。

## 前端

- [ ] 删除「业务视图」筛选和事件依赖；保留其他筛选。
- [ ] 将「跑量视图」改为「跑量统计」，「校验并保存」改为「保存」。
- [ ] 移除钱包明细、钱包账户、代收／代付配置的「申请记录」快捷入口。
- [ ] 四处复用资金调整表单：锁定商户和钱包，提供手动充值／扣款、金额、原因、2FA、二次确认与结果反馈。
- [ ] 成功后同步列表、钱包明细及业务配置的余额，取消／成功返回原入口。
- [ ] 实现多条 API IP 白名单增删改；接入对应保存与校验结果。
- [ ] 代收／代付回调 IP 分别可编辑及保存，支持多 IP、清空、校验与回显。

## 后端与安全

- [ ] 配置按租户、商户、业务隔离；API 白名单和两类回调 IP 独立存储，不能互相覆盖。
- [ ] 服务端校验操作权限、金额精度、余额、原因、真实 2FA 和 IP；不依赖前端校验。
- [ ] 人工记账原子更新总额和可用余额，冻结金额不变；实现并发控制及幂等，失败不产生部分记账。
- [ ] 持久化调整流水及审计：商户、钱包、操作类型、金额、前后余额、原因、操作人、时间、关联编号。
- [ ] 保存配置变更前后值及审计；接入对应业务的回调配置消费逻辑。
- [ ] 开发前确认现有服务如何使用「回调 IP」（接收目标或网络配置），沿用现有回调 URL 契约；本次不另建回调协议。

## 测试

- [ ] 四处入口、三个钱包、充值／扣款、取消返回及各处余额一致性。
- [ ] 零／负数／超过两位小数、余额不足、原因空、错误 2FA、余额并发变化及重复提交。
- [ ] 白名单多项增删、IPv4／IPv6／网段、清空含义、错误及重复项；回调 IP 拒绝网段。
- [ ] 两类回调 IP 与 API 白名单互不覆盖、不同商户隔离；未开通业务不可编辑。
- [ ] 删除快捷入口后仍可从侧栏查看商户资金申请；列表检索、窄屏及键盘操作回归。

## Demo 交付与复验

- 唯一界面源：`public/legacy/sources/merchants.html`；不新增平行 Demo。
- 回归脚本：`scripts/merchant-access-smoke.cjs`；现有业务冒烟：`scripts/dist-smoke.cjs`。
- 检查：`npm run build` → `npm run check` → 两个浏览器回归脚本，参数为构建后的 `dist/index.html` 绝对路径。
- 模拟状态仅在当前页面会话内保留；固定模拟 2FA 不得用于生产。浏览器回归需本机安装 Chrome。
- [在线统一 Demo](https://jeffcity.github.io/huisheng-pay-tenant/)，入口：商户运营 → 商户管理。
