# 开发与测试任务摘要｜租户端通知中心

## 交付目标

在「运营中心 → 通知中心」保留一个侧栏入口，提供两个一级页签：

- 发送的消息：作为当前租户的 TG 推送历史，只展示推送时间、推送事件、推送内容。
- 通知配置：维护当前租户的通知目标、机器人、模板、启停状态与特殊预警参数。

## 开发任务

### 前端

| 工作项 | 开发内容 | 完成标准 |
|---|---|---|
| 1. 页面入口 | 保留「运营中心 → 通知中心」，实现「发送的消息」「通知配置」两个一级页签。 | 默认打开「发送的消息」，页签切换正常。 |
| 2. TG 推送历史页 | 实现事件、TG 群 ID、日期筛选；实现刷新、重置、导出。 | 列表只展示推送时间、推送事件、推送内容三列；正文保留换行。 |
| 3. 通知配置页 | 实现新增、修改、启停、删除、模板变量校验和预览。 | 必填校验、未定义变量校验、删除原因校验均有明确提示。 |
| 4. 特殊预警配置 | 实现恶意拉单预警的统计窗口、拉单次数、IP 数量阈值维护。 | 只接受大于 0 的整数，保存后仅影响当前租户。 |
| 5. 权限与状态 | 按后端返回的当前租户与角色权限控制页面和操作按钮。 | 无查看权限不可进入；无维护权限不可修改配置。 |

### 后端

| 工作项 | 开发内容 | 完成标准 |
|---|---|---|
| 1. 通知配置服务 | 提供当前租户的配置查询、新增、修改、启停、删除和专属预警参数维护。 | 所有写操作限定当前租户并写入审计记录。 |
| 2. 模板校验与渲染 | 校验模板变量必须属于所选消息类型；在实际推送时渲染最终正文。 | 不支持的变量不能保存；推送正文可与模板预览核对。 |
| 3. TG 推送执行 | 在业务事件触发后匹配当前租户全部启用配置，并向对应 TG 群推送。 | 同一事件配置多个群时，每个群均收到一条消息。 |
| 4. 推送历史留存 | 每次实际推送留存事件、接收群、推送时间、模板快照、变量快照和最终消息内容。 | 历史列表可按筛选条件返回最终正文。 |
| 5. 历史不可改写 | 配置修改、停用或删除后不修改已经留存的推送正文。 | 历史正文与推送时内容一致。 |
| 6. 租户隔离 | 配置、历史和导出查询均以当前租户为边界。 | 任一租户不能读取、导出或修改其他租户数据。 |

## 建议的数据口径

### 通知配置

- 租户标识、消息类型、TG 群 ID、通知机器人、风险等级、消息模板、启停状态。
- 创建和更新时间；创建人、更新人。
- 恶意拉单预警的专属阈值。

### TG 推送历史

- 租户标识、关联配置、推送事件、TG 群 ID、推送时间。
- 推送时模板快照、变量快照、最终消息内容。
- 推送结果和失败说明可用于后台追踪，但租户端列表不展示内部投递字段。

## 关键业务约束

1. 发送的消息列表固定只展示：推送时间、推送事件、推送内容。
2. 列表内容是最终发到 TG 群的正文，不是当前配置模板的实时预览。
3. 模板、变量、群目标或启停状态被修改后，已存在的历史消息不能被重算或覆盖。
4. 同一事件配置多个 TG 群时，每个群产生一条独立推送历史。
5. 删除配置只阻断后续推送，不删除历史。
6. 所有查询、导出和配置操作必须限定当前租户。

## 测试任务

### 功能测试

| 场景 | 测试动作 | 预期结果 |
|---|---|---|
| 页面入口 | 进入通知中心并切换两个页签。 | 默认展示发送的消息；两个页签均可正常打开。 |
| 历史字段 | 检查任意历史记录。 | 仅有推送时间、推送事件、推送内容三列。 |
| 消息一致性 | 触发商户资金划转、钱包地址变更、代付订单驳回、恶意拉单预警和登录异常。 | TG 群正文与页面历史正文逐条一致。 |
| 模板快照 | 推送一条消息后修改该事件模板，再次触发相同事件。 | 新消息使用新模板；旧消息正文不变。 |
| 多群推送 | 为同一事件配置两个 TG 群并触发事件。 | 两个群各收到一条消息；页面生成两条历史。 |
| 历史筛选 | 分别及组合设置事件、群 ID、日期。 | 返回记录和导出文件均与筛选条件一致。 |
| 配置维护 | 新增、修改、启停、删除配置。 | 各操作结果正确；删除必须填写不少于 4 个字的原因。 |
| 模板校验 | 输入空字段、未定义变量。 | 页面阻止提交并提示对应字段。 |
| 专属预警 | 输入 0、负数、有效正整数。 | 非正整数不能保存；有效值保存成功。 |

### 权限与数据测试

| 场景 | 测试动作 | 预期结果 |
|---|---|---|
| 查看权限 | 分别使用有查看权限、无查看权限账号访问。 | 有权限账号可查看；无权限账号不可访问数据。 |
| 维护权限 | 使用仅查看账号尝试新增、修改、启停、删除。 | 所有维护操作被拒绝。 |
| 租户隔离 | 使用两个租户分别查询、导出和维护。 | 任一租户均不能看到或操作另一租户数据。 |
| 审计记录 | 完成新增、修改、启停、删除。 | 审计记录包含操作人、时间、操作类型；删除包含原因。 |

### 异常与回归测试

| 场景 | 测试动作 | 预期结果 |
|---|---|---|
| 无配置 | 使用无通知配置租户触发事件。 | 不产生 TG 消息；页面历史不新增记录。 |
| TG 推送失败 | 模拟机器人或群不可用。 | 记录失败原因，不影响原业务事件结果。 |
| 长消息 | 使用包含多行和长变量值的模板触发推送。 | TG 与页面均完整展示，页面不覆盖相邻列。 |
| 配置删除后 | 推送历史存在时删除关联配置。 | 历史仍可查询；后续同事件不再向该群推送。 |
| 既有功能回归 | 检查通知中心外的核心导航和资金相关页面。 | 原有页面可进入，既有关键流程保持可用。 |

## 本次不包含

- TG 机器人和群的平台注册、授权与创建。
- 已发送 TG 消息的撤回、编辑或人工重发。
- 平台全局风控策略修改。
