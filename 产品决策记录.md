# 产品决策记录

## 2026-09-02｜租户端通知中心整合通知配置

- 触发背景：参考蓝盛「TG 系统消息配置」能力，为汇盛租户端补充租户自己的通知配置入口。
- 讨论方案：曾考虑在「运营中心」新增独立菜单「通知配置」。
- 最终决策：不新增侧栏菜单，保留并替换原「运营中心 → 通知中心」页面；页面内设置两个一级页签：
  1. 「发送的消息」：默认页签，作为已发送至 TG 机器人的历史回看，只展示推送时间、推送事件和推送内容；内容按推送时命中的模板快照渲染。
  2. 「通知配置」：参考蓝盛配置能力，提供消息类型、TG 群、机器人、风险等级、消息模板、变量预览、启停、编辑、删除与特殊预警参数配置。
- 租户化口径：页面及配置数据仅作用于当前租户；通知类型和文案采用租户业务语义，不直接照搬平台管理端命名。
- 历史数据口径：删除、停用或后续修改配置不会删除或改写「发送的消息」中的历史记录和历史内容。
- 导航影响：侧栏仍只有一个「通知中心」入口，不增加导航层级，也不改变页面总数。
- 影响范围：`src/legacy/modules.json`、`public/legacy/sources/notifications.html`、项目说明、检查脚本与自动化测试。

## 2026-09-21｜租户登录区分 Google 2FA 已绑定与未绑定

- 触发问题：工程评审需要在统一 Demo 内完整体验 Google 2FA 已绑定账号与未绑定账号的不同登录结果。
- 考虑过程：单一账号无法同时表达两种绑定状态；仅在评审工具栏切换页面也无法验证账号密码登录后的真实分流。
- 最终决定：登录页提供两组独立模拟账号资料。已绑定账号在账号密码校验通过后进入 6 位验证码验证；未绑定账号进入首次绑定页面，展示模拟二维码、模拟密钥与首次绑定验证码，绑定成功后再进入有权限的页面。
- 决定原因：两条流程都从账号密码登录开始，工程与测试可以分别验证分流条件、表单反馈、绑定结果和后续路由。
- 影响文件：`public/legacy/sources/login.html`、`test/project.test.mjs`。

## 2026-09-25｜商户管理开发清单摘要

范围：租户端 → 商户运营 → 商户管理。本清单按本轮评审最终确认结果记录，不包含此前已合并的登录页改动。

### 新增与调整

- [x] **多 IP 白名单配置**：接入信息中的 API 请求 IP 白名单改为多条配置，支持新增、删除、编辑及保存回显；保留格式校验、重复项提示和变更时的 Google 2FA 验证。
- [x] **资金调整入口**：钱包明细、详情「钱包账户」、代收配置、代付配置均提供入口，自动带入当前商户及对应钱包；完成或取消后返回原入口。
- [x] **手动充值／手动扣款流程**：填写操作类型、金额、原因及 Google 2FA，确认后更新余额。当前总额与可用余额同额增减，冻结金额不变；扣款不能超过可用余额。记录调整流水和操作日志，同步各处钱包展示。
- [x] **代收回调 IP**：由只读展示改为可编辑配置，随「保存代收配置」保存。
- [x] **代付回调 IP**：新增配置字段，随「保存代付配置」保存。
- [x] **回调配置隔离**：代收、代付回调 IP 分别维护，支持多个公网 IPv4／IPv6、格式及重复校验、清空和变更日志；与 API 请求 IP 白名单独立，修改任一配置不得覆盖其他配置。

### 删除

- [x] 移除钱包明细、详情「钱包账户」、代收配置、代付配置中的「申请记录」快捷入口；用户统一从侧栏「商户资金申请」查看，原页面和记录保留。
- [x] 移除商户列表「业务视图」筛选项及关联筛选逻辑；保留商户关键词、商户类型、业务能力、商户状态、创建时间条件。

### 文案

- [x] 商户详情页签：「跑量视图」→「跑量统计」。
- [x] 接入信息按钮：「校验并保存」→「保存」，不取消原有校验。

### 验收与交付边界

- 已完成本地 Demo 源码修改；构建、项目检查和浏览器回归通过。
- 已验证多 IP 配置、资金调整各入口、余额联动、回调 IP 保存与隔离、筛选项移除及商户关键词检索。
- 本清单中的勾选表示 **Demo 已实现**，不表示生产后端已开发。当前数据为页面内模拟状态，刷新后重置；未接入真实支付通道、真实 2FA 或持久化账务服务。
- 本清单初次记录时改动仅在本地；后续发布状态以 GitHub PR 与部署记录为准。
- 需求与开发摘要：[[PRD_商户管理优化_20260925]]、[[开发任务摘要_商户管理优化]]。
- 影响文件：`public/legacy/sources/merchants.html`、`scripts/merchant-access-smoke.cjs`。
